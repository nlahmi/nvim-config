return {
  packages = {},

  mason_packages = {},

  nonls_packages = {},

  lsp_config = {
    function(_, capabilities, custom_attach)
      vim.lsp.config("CHANGEME", {
        on_attach = custom_attach,
        capabilities = capabilities,
      })
    end,
  },
}
