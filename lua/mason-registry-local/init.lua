-- A local mason registry, listed ahead of mason-org's so the packages here win.
-- Each entry is a module returning a single registry+v1 package spec.
return {
  "mason-registry-local.tree-sitter-cli",
}
